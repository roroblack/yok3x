# v4.3.0 · 계획서 — 통합 실행 로드맵 체크리스트 · 2026-07-16

**단일 진실 원천**: 흩어진 백로그(멀티에이전트 리뷰 G1~G5 · ACQUIRE S3/S4 · 부속기능)를 하나의
우선순위 체크리스트로 통합. **codex 공동검토 반영**(순서·의존성·동시성 함정). 이 파일을 기준으로 진행하고
각 항목 완료 시 `[x]` 체크 + HISTORY 기록. 구현=codex(생산자)/검토=Claude(리뷰어) 원칙 유지.

## 배제(합의) — 하지 않음
- temperature/top_p/top_k 노브(코딩 CLI 미지원 = "안 먹는 노브") · effort "ultracode"(effort 아님·클라우드 과금).

## 실행 순서 (codex 최종 권고)
`F0(측정계획) → A-lite(텔레메트리·정직한 진행률) → B(Resolver 재검증) → C(진짜 병렬) → F(A/B) → E(few-shot) → G(체크포인트) → D(앙상블) → H(MCP)`

## 기반(cross-cutting): 공통 실행상태 계약 — **최우선, A·C·G가 공유**
> codex 지적 최중요 누락. A/C/G가 각자 상태모델 만들면 재작업. 지금부터 공통 필드 확정.
- [~] 스텝/호출 공통 필드: `index · status · usage(nullable)` 착수(StepLog). `call_id·reservation`은 C에서.
- [x] `usage`는 nullable: **"$0/0 tokens"과 "측정 불가(None)"를 구분**(A-lite에서 0→None, 배지 `—`).
- [ ] 상태 파일 쓰기는 **임시파일 후 atomic replace**(동시쓰기 손상 방지) — C의 선행.

---

## F0. 측정 계획 고정 (A/B 방법론) — 문서, 코드 전 선행 ✅ 부분완료(2026-07-19: 심판 캘리브레이션 계측)
> C·F의 효과를 재현가능하게 재려면 방법론을 먼저 못박는다.
- [ ] 이슈군 선정(대표 1개 아님, 복수) · 성공 기준 · 동일 base commit/model/effort 고정.
- [ ] 지표: 성공률 · **기존성공→실패율(별도 최우선 보고)** · 토큰 · 벽시계 · 호출수 · 가드중단률.
- [ ] **"변경파일수"→"제안 파일수"**로 교체(워커가 파일 직접수정 안 함 = H 전엔 무효 지표).
- [ ] **심판 캘리브레이션**(CLIP 평가 교훈 이식 — "평가자를 객관 신호로 검증하라"):
  런마다 `(SCORE, verify_cmd 통과여부, 특징[토큰·시간·라운드·이슈수·backend·effort·pattern])` 기록 →
  **상관·혼동행렬**로 "SCORE가 실제 테스트 통과를 예측하는가" 측정. 순수 stdlib 통계. 상관 없으면
  producer-reviewer 게이트가 연극임을 뜻하므로 **최우선 확인 가치**.
  - 주의(블로커): 라벨은 `verify_cmd` 있는 작업만 · 런이 비싸 샘플 축적 느림 · **모델스택이 자주 바뀌어
    (비정상성) 옛 런 적합 모델은 금방 상함**(CLIP은 frozen 인코더라 성립, 우리는 아님).
  - **선형모델은 후속**: 데이터 충분해지면 stdlib 로지스틱 회귀로 `특징→성공확률` 예측 → 라우팅/escalate에
    활용(의존성0 가능, sklearn/numpy 불필요). **데이터 없이 먼저 ML 서브시스템 짓지 않는다**(과설계).
- [x] **심판 캘리브레이션 계측 구현**(2026-07-19): `calibration.py`(순수: 점이연 상관·혼동행렬·요약, stdlib만) +
  런마다 `.yok3x/calibration.jsonl`에 (score·verify_ok·특징) append + `yok3x calib` 요약 CLI. 데이터 수집 시작.
  A/B 이슈군·paired 실행(F)은 데이터 축적 후. 검증: 강한상관 0.98/무상관 0.0/혼동 fp 탐지/엣지 None, 205 passed.
- [x] **데이터 수집 강화**(2026-07-20, 작업B, codex 구현·Claude 검토): N0'가 드러낸 "임계 8.0 전량반려"에
  대응해 실데이터 수집을 3결함 수정. (B-1) `make_record` 미지 키 raise(오타 침묵실패 차단) ·
  (B-2) 최종 라운드만 아니라 **라운드별 관측치 전부 기록**(run_id·round 보존, SCORE 전구간 확보) ·
  (B-3) 스키마에 reviewer(실제 심판 backend)·threshold·gate_pass 추가. 검토수정: rounds(총량)/round(인덱스)
  중복 제거, 런합계(tokens·cost·issues)는 마지막 행에만(파일합산 과대계상 방지). **임계값 8.0 불변**(별도작업). 219 passed.
- [x] **라벨 누출 제거**(2026-07-20, 작업E, codex 발견·구현·Claude 검토): 리뷰어가 verify 결과를 **본 뒤**
  SCORE를 매기고 있어 (SCORE↔verify_ok) 상관이 오염돼 있었다. B로 모으던 데이터가 실은 오염 데이터였음.
  리뷰어를 blind로(산출물·rubric만) + 하드게이트·호출수 불변 + verify 실패 진단은 다음 producer로.
  리뷰어 입력의 라벨 누출은 제거했지만, verify 범위 오염은 아래 F1-b에서 별도로 격리. 222 passed.

## F1. 게이트 의미 분리 + 수정모드 (2026-07-21 신설 · PDF 교육자료 원칙 + 사용자 제안 + codex 공동설계)
> 원칙(웹리서치·5단계 검증 사다리): 객관 검증(verify=L3)과 LLM 판단(SCORE=L4)은 다른 층.
> "실제 테스트에 합격한 코드를 AI 주관 점수만으로 떨어뜨리지 말자"(N0'에서 정상코드 전량반려 실측).
- [x] **F1-a advisory 게이트**(2026-07-21, codex 구현·Claude 검토): task spec `score_gate_mode`
  (기본 `strict`, `advisory`=verify가 통과 결정·SCORE는 review_required 플래그만). advisory+verify_cmd
  없음=실행 전 RunAborted. 순수함수 evaluate_score_gate로 분리. GUI select(auto 없음). calibration
  gate_mode 기록. **F1-c 포함**(status에 gate 객체·done 의미 유지). 임계 8.0 불변. 242 passed(신규 20).
- [x] **F1-b verify 원본검증 오염 방지**(2026-07-21, 최소 조치): `verify_scope` 스키마를 추가하고
  현재 producer-reviewer 관측을 `original_tree`로 명시. 상관·혼동·요약은 `candidate`만 라벨로 인정해
  기존·현재 오염 관측을 격리했다. 게이트 동작은 불변이며, 유효 라벨 축적은 **F1-d 스테이징이 선행**.
  244 passed(신규 2).
- [x] **F1-c `_finish` 게이트 결과 미보존**(2026-07-21, F1-a에 포함 완료): status에 gate 객체 기록.
- [x] **F1-d 수정모드(review bundle) MVP**(2026-07-21, codex 구현·Claude 검토): task `changes:{"mode":"review"}`
  opt-in. 기존 파일 대비 `difflib.unified_diff` → `yok3x-out/<run_id>/`에 candidate+changes.diff+changes.json
  (base/proposed sha256·status) 게시. **읽기전용**(workdir 원본 불변, E2E 실측 확인)·고정 격리 root·CLI만.
  잔여(별도 후속): ①후보 스테이징+verify(verify_scope=candidate=T-1 선행) ②수락/거절/개선 인터랙션+worktree 적용 ③부분(hunk)적용 ④GUI diff viewer.
- [x] **F1-e GUI 빈 `catch(e){}` 10곳**(2026-07-21): 설정저장/토글 fetch 실패가 조용히 묻히던 것 →
  `toast()` 헬퍼 추가(3초 실패 알림) + console.warn. 실브라우저 검증: 서버 종료 상태서 saveGuardRatios
  실패 시 toast "서버 연결 실패 — 반영되지 않음" 표시. 244 passed.

## 1. A-lite — 텔레메트리 + 정직한 진행률 (G5) · GUI+guiserver+orchestrator ✅ (2026-07-16)
> 데이터는 이미 `step_NN.json`(total_tokens·cost_usd·duration_ms)에 있음. 노출만.
- [x] `StepLog`에 nullable `tokens·cost_usd·duration_ms`(0/누락→None=미측정 구분). status.json 자동 직렬화.
- [x] `/api/state`(build_state) stepdetail이 tokens·cost_usd·duration_ms 노출.
- [x] 채팅 스텝 카드 배지 `⏱시간(항상)·🔢토큰·💲비용(보고 시만, 없으면 —)`. 옛 런은 — 표시(정직).
- [x] 진행률: 런 메타에 `done/steps 스텝`(총 계획수는 패턴 가변임을 tooltip 명시). **가짜 진행바 없음.**
- [ ] (C와 함께) `queued/running/done` 실시간 상태 + `planned_steps` 확정 패턴 `n/N`. — C에서.

## 2. B — ACQUIRE S3a: Resolver 수정 전 재검증 · orchestrator+acquire ✅ (2026-07-16)
> 오고정 회귀(논문 5건) 완화. **LLM 호출 증가 0** — 파일시스템 기계검증이라 무료·결정적.
- [x] `acquire.core_claim()`: 결정적 SHA-1 `claim_id`(hashlib) + 핵심주장 1개. 재현성 보장.
- [x] `orchestrator.verify_evidence()`: evidence의 path 존재·symbol 등장을 **호스트 파일시스템에서 확인**
      (행번호는 검증 안 함 — 논문상 세부오류 대부분이 행번호). LLM 호출 없음.
- [x] `acquire.apply_verdicts()` 순수함수: path없음→**contradicted(폐기)** / symbol없음→**partial(위치힌트만,
      downgraded)** / 둘다→**confirmed**.
- [x] `render_qa_context`: contradicted 렌더 제외 · partial에 "위치 힌트로만" 명시 · 기계검증 완료 항목 표시
      (Resolver 중복확인 방지).
- [x] `acquire.json`에 판정·claim_id·evidence_check·dropped 이유 저장(감사). 요약 로그.
- [x] 재검증 실패/예외 시 기존 QA로 본 수리 계속(fail-safe, 회귀 없음).
- [ ] 파일변경/commit 변화 시 재검증 재요구 — **후속**(현재는 preflight 시점 1회).

## 3. C — 진짜 병렬 `call_workers_parallel()` (G1) · **최대 난이도, 동시성 안전 필수**
> fanout·ACQUIRE Answerer 공용 인프라. 아래 함정 전부 처리 전엔 기본 비활성.
> **하위 단계로 분할 진행**(각 단계 독립 배포·리뷰 가능):
> - **C-1** ✅(2026-07-16) 준비/실행 분리(`CallSpec`) + status/acquire **원자적 쓰기**(tmp→`os.replace`).
>   `prepare_call`(부작용0·결정적: step_i 불변·파일생성X·게이트 미호출) / `execute_call`(가드·게이트·실행).
>   `call_worker`=조합이라 **동작 불변**. `_isolated_cwd_path()`로 경로결정/디렉터리생성 분리. 114 passed.
> - **C-2** ✅(2026-07-16) `yok3x/reserve.py`: 프로세스간 **lockfile**(`O_CREAT|O_EXCL`·stale TTL 회수) +
>   **예약 원장**(`reservations.json`, 원자적) + `reserve/release/cleanup_stale`. **TOCTOU 방어 실측 확인**
>   (각 150<200 통과하나 합 300>200 → 2차 거절 / 각 $3<$5이나 합 $6>$5 → 거절). `approve_batch`(제어
>   스레드 1회 승인, worker `input()` 금지) + `reserve_and_approve`(거부 시 예약 해제=누수 없음).
>   `CallSpec.batch_approved`. 설정은 `guard.reservation`(RULE §5.5). **calls=정확예약, 토큰·USD=보수적 추정.**
>   스레드 미도입. 125 passed.
> - **C-3** ✅(2026-07-17) `call_workers_parallel()` — **기본 비활성**(`guard.parallel.enabled=false`,
>   꺼지면 순차 폴백=기존 동작). 제어 스레드서 `reserve_and_approve`+step index 선할당 → worker는 실행만
>   (게이트 호출 0회 실증). 입력순서 반환 · all-settled(실패 슬롯만 None) · backend별 Semaphore 상한 ·
>   **취소 계약 실구현**(Popen 추적 → `_terminate_active_processes`, Future.cancel로 못 죽이는 CLI를 실제 종료) ·
>   finally에서 `reserve.release`(성공·예외 모두 해제 실증). 실측: 4건×0.35s → 0.50s(순차 1.40s), 동시최대 4;
>   per_backend=2면 동시최대 2. 164 passed.
> - **C-4** ✅(2026-07-18) ACQUIRE Answerer를 `call_workers_parallel`로 이식(병렬 인프라 첫 실사용자).
>   질문마다 `prepare_call(read_only=True)` → 병렬 실행 → 질문과 zip해 순서 보존 처리. 실측 병렬 0.46s
>   vs 순차 1.40s(3배). 순서보존·read_only유지·all-settled·양경로 동일결과 실증. 175 passed.
> - **C-5** ✅(2026-07-18) 일반 fanout을 `call_workers_parallel`로 이식. 워커마다 prepare_call(initial_context는
>   첫 워커만) → 병렬 → 워커와 zip해 `### w` 순서 보존. fan-in(join_worker)은 단일이라 순차 유지. 실측 병렬
>   0.57s vs 순차 1.17s(2배)·동시최대 3·순서 보존. flaky 타이밍 테스트(sum*0.75)를 견고화(동시진입 카운터+<합).
>   **→ C 블록 전체 완료(C-1~C-5).** 182 passed.
- [ ] `CallSpec` 준비 분리(라우팅·모델·cwd·read-only 결정) — 실행과 분리.
- [ ] **배치 승인**: 워커·백엔드·예상상한을 한 번에 표시(worker thread에서 `input()` 금지, 제어 스레드서 선완료).
- [ ] 단계번호 범위 + **비용 예약을 잠금 아래 선점**(정확예약=호출수, 추정예약=토큰·비용 구분).
- [ ] `ThreadPoolExecutor` — 결과는 **입력 순서**로 반환. **backend별 동시성 상한** + 전역 max_workers.
- [ ] usage·steps·log·status 쓰기 동기화 + atomic replace.
- [ ] **TOCTOU 방지**: 각 스레드 개별 가드 통과 후 합산이 hard limit 넘는 것 차단(예약 기반).
- [ ] **프로세스간 경쟁**: stdlib lockfile + stale 복구(GUI·별도 CLI 동시 실행 대비).
- [ ] **취소 계약**: Future 취소가 실행중 CLI 프로세스를 실제 종료하도록.
- [ ] **all-settled**: 한 Future 실패가 다른 결과 삭제 안 함. 부분성공 허용.
- [ ] **예약 누수**: pending 예약 TTL·run ID·크래시 복구.
- [ ] 이식 순서: **ACQUIRE Answerer 먼저 → 안정화 → 일반 fanout**. 동시실패·timeout·Ctrl-C·부분성공 테스트 후 활성.

## 3.5 ✅(2026-07-17) claude 추정 자동 캘리브레이션 + 토큰 수 표시 [사용자 요청]
> **실측이 죽으면 사용량이 원장($0/$5)으로 떨어져 무의미해지는 실제 통증.** 원인: 트랜스크립트 추정이
> **캐시 read 토큰까지 합산**해 ~150배 과대(측정: 7d 38억tok=768% vs 실측 5%) → 200% 가드가 버림(BUG-15 방어).
> 해결 원리 = CLIP 교훈과 동일: **실측(지상진실)으로 추정기를 보정**한다. 수동 `calibrate`(`cap=토큰/(실제%/100)`)를
> **실측 성공 시마다 자동 수행**하면, 실측이 죽어도 보정된 추정이 나온다.
- [x] `limits.autocalibrate_claude()`: 라이브 성공 시 창별 `cap = rolling_tokens / (live_pct/100)` 역산 →
      `limit_5h_tokens`·`limit_7d_tokens` 저장. **가드**: live_pct가 너무 작으면(<1%) 건너뜀(0 나눗셈·폭주 캡 방지),
      쓰기 rate-limit, 캡이 비현실적이면 무시. 기존 수동 `calibrate` 로직 재사용.
- [x] `_probe_claude_oauth` 라이브 성공 경로에 연결(부작용은 캘리브레이션 저장뿐).
- [x] **토큰 수 표시**: Window에 used/limit 토큰 노출 → GUI가 `%` 뿐 아니라 **사용/남은 토큰량** 표시.
- [x] Fable 등 per-model 창은 라이브 전용임을 명시(트랜스크립트 per-model 분해는 범위 밖).
- [x] 보정 후 추정이 200% 가드 통과 **실측 대조 확인**: 768%→16.0%/5.0%로 **실측과 0.0%p 일치**.
      ⚠ 리뷰서 발견·수정: 스펙의 '100배 초과 무시' 가드가 **정상 보정(7d는 cache read로 ~153배)을 거부**했음
      → `max_calib_multiple`(기본 1000) 설정으로 분리(§5.5, 하드코딩이기도 했음). 회귀 테스트 2개 추가..

## 4. F — ACQUIRE S4 A/B 파일럿 (F0 실행)
- [ ] F0 방법론대로 paired A/B(같은 base). 기존성공→실패율 최우선 보고. 복수 이슈 반복.

## 5. E — few-shot 예시 필드 · task spec+GUI (경량)
- [ ] task spec 필드 + 문자 상한. **역할범위: 기본 Resolver/build만**(Questioner/Answerer 주입 금지=조기가설 방지).
- [ ] 프롬프트 주입 + 로그 표시 + GUI 편집필드.
- [ ] 예시 안 명령·주장은 **신뢰데이터 아닌 사용자 입력**으로 취급(인젝션 주의).

## 6. G — 체크포인트/재개(최소) (G4) · **D보다 먼저**(병렬·앙상블이 런을 길고 비싸게 함)
- [x] **G-1 ✅(2026-07-20) 순차 pipeline 한정 재개**(codex 설계검토 반영 — 범위 최소화):
  · **선행 수정**: 가드 stop이 바깥 핸들러의 `aborted`에 덮여 원인이 기계판독 불가하던 버그 →
    `RunAborted.cause` + status에 `cause`·`resumable` 저장(실측: `cause=guard_stop, resumable=True`).
  · **call_key**(내용 해시, 인덱스 아님 — escalate/failover로 시퀀스가 달라질 수 있어서) + **strict
    manifest**(spec_sha256·워커 backend/model/effort·flavor·verify_cmd 일치할 때만) + **성공 prefix만** 재생.
  · **verify_cmd는 절대 재생 안 함**(파일·환경 변경 가능 → 항상 재실행). 재생분 **usage 이중집계 없음**.
  · lineage 잠금(동시 재개 차단)·손상 step 파일 방어·실패/blocked는 캐시 안 함(재시도).
  · 실측: 3스테이지 중 2번째 중단 → 재개 시 **s1 재생(호출 0)·s2·s3만 실제 호출**, usage.record 2회(이중 없음).
    spec 변경/없는 런/producer-reviewer 패턴은 **거부**. 216 passed.
- [ ] **G-2 후속**: producer-reviewer 재개(라운드 연속 prefix + escalate/stall 상태 재계산) → 병렬 fanout 재개.

## 7. D — 앙상블 best-of-N + 심판 (G3) · C 위에
- [ ] 후보 스키마 + N 상한. **temperature 대신 모델·effort·프롬프트 앵글 다양화**.
- [ ] 후보 익명화·순서 셔플(심판 편향 완화). **심판 몫도 후보 실행 전 선예약**. 심판 실패 fallback.
- [ ] 측정된 작업군에서만 opt-in.

## 8. H — MCP 워커도구 (G2) · 최후, 안전 최대
- [ ] **read-only 먼저 → 격리 쓰기(worktree)** 나중. **병렬 쓰기 워커는 격리·충돌정책 전엔 금지**(C와 결합 금지).

## N. 나이틀리 자가개선 루프 [사용자 요청·codex 공동검토] — 계획서 `plans/v5.0.0-plan-nightly-self-improvement`
> **"스스로 완성" 아님**: 문제 견인형 연구 → 격리된 1가설 섀도 구현 → 독립 A/B → **사람 승격**.
> 자동 채택은 게이트(교정된 F0·회귀0·불변 보호영역·자원상한·복잡도예산) 전엔 **금지**.
- [ ] **N0(최우선)** F0 backfill — **과거 패치(BUG-19/21/22/23·ACQUIRE 회귀)를 baseline/candidate 쌍으로 재생**해
      "SCORE가 실제 성공·회귀를 구분하나" 지금 측정(데이터 0 탈출) + holdout 확인.
      **심판이 미교정으로 판명되면 자동화 전면 보류가 답.**
- [ ] **N1** 섀도 나이틀리 1회 완주: 주제1·논문≤3·**가설1**·비보호모듈1·신규의존성0·비용/시간 고정·
      폐기가능 작업공간·verify+paired A/B·**자동merge 없이 증거보고서만**·실행 후 정지.
- [ ] **N2** 루틴화(미검토 후보 있으면 생성 정지=리뷰피로 방지, 부정결과 보존).
- [ ] **N3** 자율 승격 — **지금은 금지**. N2 장기 안정 후에만 논의.
- 하드 경계: guard/reserve/승인/페이싱/limits **및 기본값** 수정 금지(불변식 테스트로 간접 약화도 차단) ·
  논문 fetch는 읽기전용·repo 스크립트 실행 금지(공급망) · 가져온 텍스트는 **데이터로만**(인젝션 방어).

## T. 자동 트리아지 (착수 전 규모/티어 판단) [사용자 요청·codex 공동검토]
> 근거 리포트: `reports/v4.x-assessment-auto-triage-2026-07-18.md`. **핵심: 규모≠위험** — 복잡도·실패
> 영향도·검증가능성·비용을 분리. escalate(실패신호 기반)는 유지하고 트리아지는 그 앞의 저비용 시작점만 고름.
- [ ] **T1** `estimate_execution(spec)` 순수 규칙(4축) → `{pattern,tier,max_rounds,skip_review,신뢰도,근거}`
      **추천 로그만**(자동 적용 X). 무료·의존성0·호출0. GUI에 추천 배지.
- [ ] **T2** 저위험·무과금 경로만 opt-in 자동(`direct ↔ local 1-shot`). 유료 승급·검토 생략 제외.
- [ ] **T3** 예산 게이트(사용자 사전설정 내 유료 자동 승급, 초과/외부쓰기=승인). C-2 예약 재사용.
- [ ] **T4** F0 데이터로 규칙 임계 보정 + 데이터 충분 시 애매구간만 로컬모델 하이브리드.
- 금지: 지금 단일 로컬모델 전권 트리아지(과설계) · 네 결정 동시 자동화(오판 연쇄) · 승인 없는 유료 승급.

---
## 진행 규율
각 항목은 **독립 배포 가능한 최소 단계**로. 완료 시 이 체크리스트 `[x]` + HISTORY + (해당 시)bugs/·ADOPTIONS.
