# v4.6.0 · T-2 2차 데이터 수집 프로토콜 **사전등록**(라운드 2) · 2026-08-10

**목적**: [1차 결과](v4.5.0-t2-collection-round1-results-2026-07-27-1240.md)가 `verify_ok=False`
0건(기준 ≥3)으로 T-1 착수 조건 미충족 — 작업군이 claude-main에게 너무 쉬웠다. 2차는 **난이도를
확실히 올린 작업군**으로 재수집한다. 작업군 설계는 codex(gpt-5.6-luna)와 실제 상담(2026-08-10,
토큰 8,573)해 확정 — 세션 로그 보존.

**이 문서는 데이터 수집 착수 전에 작성됐다. 결과를 본 뒤 이 문서의 기준을 바꾸지 않는다.**
(CLIP 교훈: 사후 최적화 금지 — 1차와 같은 원칙.)

---

## 1. 고정 조건 (1차와 동일 — 재현성 유지)

| 항목 | 고정값 | 비고 |
|---|---|---|
| 패턴 | `producer-reviewer` | |
| 프로듀서 | `claude-main` (backend=claude) | |
| 리뷰어 | `codex-critic` (backend=codex) | |
| effort | 설정 기본값(미지정) | |
| `max_rounds` | **2** | 1차와 동일 |
| `pass_score` | **8.0** | |
| `score_gate_mode` | `strict` | |
| `verify_cmd` | `python -m pytest -q` | 작업별 전용 테스트 파일(R-6 보호로 프로듀서가 수정 불가) |
| `changes.mode` | `review` | 원본 트리 불변 |
| `workdir` | 작업별 독립 디렉터리(필수, BUG-40) | |
| 코드 base | 이번 라운드 착수 시점 HEAD 커밋 | 수집 중 yok3x 코드 변경 금지 |

**신규(1차 대비 변경)**: `guard.reservation.max_usd_per_run = 1.50`(codex 권고 — 1차 평균 $0.68의
약 2.2배는 허용하되 $3.37급 꼬리 런은 차단). **예산 상한으로 중단된 런은 `verify_ok=False`가 아니라
`censored`로 별도 표시한다**(codex 지적: 안 그러면 "예산이 부족해서 실패"와 "코드가 틀려서 실패"가
섞여 실패 표본이 오염된다). censored 런은 §3의 통과/실패 집계에서 **제외**하고 별도 보고만 한다.

## 2. 작업군 (12개, codex 설계 — 3개 중간·6개 어려움·3개 매우어려움)

목표: **claude-main이 max_rounds=2 안에 종종(전부는 아니게) 실패**하는 난이도. 1차 결과보고서가
제안한 6개 축(동시성/락·부동소수 정밀도·대용량 성능·상태기계 엣지·유니코드 정규화·재귀/메모이제이션)에서
고르되, 전부 실패하면 tp=0이 되므로 실패확률이 극단(0%/100%)에 가깝지 않은 조합으로 섞었다.

| # | 작업 | 함정 / 축 | pytest 검증 핵심 | 등급 |
|---|---|---|---|---|
| 1 | 정확한 금액 분배기(비율별 센트 배분, 최대잔여법) | `float` 오용·반올림 잔여·동률 순서 — **부동소수 정밀도** | 0.01 분배, 10개 동률, 큰 비율, 합계 보존 | 중간 |
| 2 | 유니코드 사용자명 정규화·중복 제거(NFKC/casefold) | NFC/NFKC 혼동·`lower()` 오용·결합문자/ß/전각 누락 — **유니코드 정규화** | 전각/반각, ß/ss, 조합형·분해형, 빈 문자열 | 중간 |
| 3 | JSON Patch 부분 적용기(add/remove/replace/move/copy/test) | 배열 인덱스·`~0/~1` 이스케이프·root 교체·실패 시 원본 훼손 — **상태기계 엣지** | 중첩 배열, `/-`, root path, 존재 안 하는 경로, 실패 후 atomicity | 중간 |
| 4 | 증분 프레임 프로토콜 파서(escape·길이·CRC, 재동기화) | chunk 경계 가정·escape 처리 순서·CRC 범위·부분 프레임 상태 — **상태기계** | 1바이트씩 전달, escape 경계분할, 잘못된 CRC 후 정상 프레임, 최대길이 | 어려움 |
| 5 | 외부 병합 정렬기(메모리 제한, 임시파일 병합, 안정정렬) | 전체 메모리 적재·임시파일 누수·동일key 안정성·빈 입력 — **대용량 성능** | 10만 행, 중복 key, 이미 정렬/역순, 메모리 상한, 예외 시 cleanup | 어려움 |
| 6 | 이벤트 로그 상태 재생기(중복·재전송·부분순서 처리) | 중복 이벤트 비멱등·잘못된 순서 적용·replay/live 불일치 — **상태기계 엣지** | 중복/역순 이벤트, 취소 후 결제, 종료된 객체, 동일 로그 재생 | 어려움 |
| 7 | 재귀 표현식 평가기(변수·공유 하위식·순환 참조, memoization) | 매번 재계산·순환 무한루프·깊은 재귀·캐시 키 오류 — **재귀/메모이제이션** | 5천 단계 chain, 공유 DAG, cycle, 재귀 호출 횟수 검사 | 어려움 |
| 8 | 결정적 DAG 작업 스케줄러(우선순위·이름 tie-break, cycle 보고) | indegree 갱신 오류·cycle 일부 탐지·비결정적 순회 — **상태기계/성능** | 여러 cycle, 분리 DAG, 동률 우선순위, 1만 노드, 결과 순서 고정 | 어려움 |
| 9 | 구간 자원 할당기(수용/거절, 취소·재할당·경계접촉) | 반개구간 혼동·취소 후 인덱스 불일치·O(n²) 탐색 — **상태기계/대용량 성능** | end==start 겹침, 동일 시작, 5만 구간, 반복 취소/재삽입 | 어려움 |
| 10 | 동시성 TTL LRU 캐시(thread-safe, single-flight) | lock 범위·만료 정리·예외 전파·deadlock·중복 계산 — **동시성/락** | `threading.Barrier` 동시 miss, fake clock, 예외, eviction, 재진입 | 매우 어려움 |
| 11 | 취소 가능한 bounded worker pool(timeout·shutdown) | queue deadlock·취소 race·worker 누수·timeout 후 결과 오염 — **동시성/락·상태기계** | Event/Barrier 결정적 테스트, queue full, 취소, shutdown 중 submit | 매우 어려움 |
| 12 | 대용량 의존성 기반 증분 빌드기(hash+dependency graph) | hash 캐시 무효화·transitive 누락·삭제 처리·cycle recursion — **성능·재귀** | 1만 노드 chain/DAG, leaf/중간 변경, 파일 삭제, cycle | 매우 어려움 |

**테스트 설계 원칙(codex 권고)**: TTL/timeout은 `time.monotonic` 주입으로 실제 대기 없이 테스트.
동시성은 `threading.Barrier`/`Event`로 결정적 동기화(sleep 기반 타이밍 테스트 금지 — flaky 방지).

## 3. 측정 지표 (1차와 동일)

- 유효 관측: `calibration.jsonl`의 `verify_scope=="candidate"` 행만.
- 주 지표: 라운드별 SCORE(blind) ↔ `verify_ok` 상관 + 2×2 혼동행렬(임계 8.0).
- 보조: 게이트 통과율(N0′ 우려 재현 여부), `stop_reason` 분포, run_id별 토큰·USD.
- **신규**: `censored`(예산 상한 중단) 건수를 별도 집계. 30% 넘으면 §5 중단조건에 준해 재검토.

## 4. 성공/회귀 판정 (1차와 동일 기준 재사용 — 사후 변경 금지)

- **T-1 착수 가능**: 유효 관측이 독립런 ≥10 · `verify_ok=True` ≥3 · `verify_ok=False` ≥3
  (`censored`는 양쪽 집계에서 제외).
- **N0′ 우려 재현**: `verify_ok=True`인데 SCORE<8.0인 관측이 3건 이상.
- **심판이 쓸모 있다고 봄**: `verify_ok=True`의 SCORE 중앙값이 `verify_ok=False`보다 1.0점 이상 높음.
- **임계 재설정은 이번 표본으로 확정하지 않는다**(1차와 동일 원칙).

## 5. 중단 조건 (1차 + censored 조건 추가)

1. **쿼터**: `yok3x pace --exit-code`가 4(stop) 또는 claude/codex 중 하나가 일 상한 초과.
2. **하네스 결함**: `verify_scope=="original_tree"`가 전체 30% 초과.
3. **비용 이탈**: 실측 누적 비용이 **$15 초과**(1차 $8.10 대비 12개 작업이 더 어려워 상승 예상,
   여유를 둠 — `max_usd_per_run=1.50` 상한과 별개로 총액도 감시).
4. **반복 실패**: 연속 3런이 `producer_failed`(백엔드 오류).
5. **censored 과다**: `censored` 비율이 30% 초과 — `max_usd_per_run=1.50`이 너무 빡빡하다는 신호,
   중단 후 상한 재검토(사전등록 위반 아님 — 이건 데이터 품질이 아니라 하네스 파라미터 조정).
6. **수집 페이싱(2026-07-27 사용자 결정 유지)**: 하루에 일정량씩 나눠 진행, 한 번에 몰아 돌리지 않는다.
7. 사용자 중단 지시.

## 6. 범위 밖 (1차와 동일)

작업E A/B, 임계 재설정 확정, 모델·effort 비교는 이번 표본으로 하지 않는다.

---

**작성 시점 근거**: 1차 실측 결과 + codex 설계 상담(2026-08-10, gpt-5.6-luna, 세션
`019fe9b4-f353-70e0-9dfc-a0d077aedeae`) 기반. 실제 작업 파일(`task.json`)·pytest 검증 파일은 이
사전등록 이후, 이 표를 그대로 구현해 작성한다(설계를 먼저 잠그고 구현은 그 다음 — 사후 난이도
조정 방지).
