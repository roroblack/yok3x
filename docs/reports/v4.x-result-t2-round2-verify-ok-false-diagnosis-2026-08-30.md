# v4.x · 결과 — T-2 2차 수집에서 `verify_ok=False`가 한 건도 안 나온 이유 진단 · 2026-08-30

## 배경

T-2 2차 수집(`docs/reports/v4.6.0-t2-protocol-preregistration-round2-2026-08-10.md`) 12개 작업이
2026-08-29 전량 완료됐다(`docs/TODO.md` T-2 참고). T-1(심판 캘리브레이션 검증) 착수 조건 중
`verify_ok=False ≥ 3`가 1차·2차를 통틀어 **한 건도 충족되지 않았다** — "매우어려움" 등급
과제까지도 claude-main이 결국 `python -m pytest -q`를 통과시켰다. 이 문서는 그 이유를
실제 산출물을 근거로 추적한 결과다. 새로운 API 호출·비용 지출 없이, 이미 완료된 T-2 런의
저장된 산출물(`step_XX_*.json`)만 재분석했다.

## 조사 대상

2차 수집 중 이번 세션에서 직접 실행해 원본 산출물(`step_01_claude-main.json`,
`step_03_codex-critic.json`)이 그대로 남아있는 두 작업의 **1라운드**(SCORE 6.0으로 반려된
라운드)를 표본으로 삼았다:
- #11 취소 가능 worker pool(`t2_round2_runs/11_worker_pool`)
- #12 증분 빌드기(`t2_round2_runs/12_incremental_builder`)

(#8·#9는 1라운드에 바로 score=9.5로 통과해 반려된 저점 라운드 자체가 없어 표본에서 제외.
round-2 앞 8개 작업(#1~#7,#10)은 워크dir가 이번 세션 시작 전에 이미 정리돼 원본 후보 코드가
남아있지 않아 재분석 대상에서 제외했다 — TODO.md의 서술 기록(점수·verify_ok)만 남아있음.)

## 방법

1. 각 작업의 1라운드 `step_01_claude-main.json`의 `text` 필드에서 후보 코드
   (` ```file:*.py ` 블록)를 원문 그대로 추출.
2. 그 후보 코드가 **원래 acceptance 테스트**(사전 검증에 쓴 그 pytest 파일 그대로)를 여전히
   통과하는지 재확인 — 로그의 `verify_ok=True`와 일치하는지 대조.
3. `step_03_codex-critic.json`의 리뷰 텍스트에서 리뷰어가 제시한 **구체적·실행 가능한 재현
   스니펫**을 뽑아 새 pytest 파일(`test_reviewer_flagged_probe.py`)로 옮김 — 원래 acceptance
   테스트에는 없던, 순전히 리뷰어의 근거·수정지시에서만 나온 조건.
4. 그 후보 코드에 이 프로브를 돌려 리뷰어의 결함 주장이 실제로 재현되는지 확인.
5. **대조군**: 같은 프로브를 T-2 실행 전 내가 직접 손으로 짜서 검증에 썼던 "정답" 참조 구현에도
   그대로 돌려, 참조 구현은 방어돼 있는지 확인.

## 결과

| 작업 | 원래 테스트(verify_ok) | 리뷰어 프로브 재현 여부 | 내 참조 구현도 같은 결함? |
|---|---|---|---|
| #11 (max_workers=0 → result(timeout=None) 영구 대기) | 통과(14/14) | **재현됨** | **예 — 내 참조 구현도 검증 안 함** |
| #12 (dep만 mark_built해도 app이 fresh로 오판) | 통과(15/15) | **재현됨** | **예 — 내 참조 구현도 동일 논리** |
| #12 (해시 불변·deps만 변경 시 미감지) | 통과(15/15) | **재현됨** | **예 — 내 참조 구현도 동일 논리** |

3건 모두 리뷰어의 결함 주장이 실제로 재현됐다(참/거짓 조작 없이 그대로 실행해 확인). 그리고
**3건 모두 내가 사전에 손으로 짜서 "정답"으로 검증에 썼던 참조 구현에도 똑같이 존재한다.**

## 진단 — 왜 `verify_ok=False`가 구조적으로 안 나오는가

이 세 가지 사례는 producer(claude-main)가 뭔가를 "빠뜨렸다"기보다, **내가 작성한 자연어
스펙 자체가 그 경우를 명시하지 않았다**는 공통 패턴을 보인다.

- #11 스펙은 `max_workers`가 "동시에 실행 가능한 최대 작업 수"라고만 했지, 0 이하일 때
  어떻게 해야 하는지 전혀 언급하지 않았다. 참조 구현도 producer도 똑같이 검증을 안 넣었다.
- #12 스펙의 `get_stale_nodes` 정의는 "**현재** 상태 기준"으로 매 호출마다 다시 계산하는
  방식이었지, "이전에 stale이었던 게 그 의존성을 실제로 다시 빌드하기 전까지는 계속
  stale로 남아야 한다"는 실제 증분 빌드 시스템의 핵심 불변조건을 명시하지 않았다. 참조
  구현도 producer도 스펙을 문자 그대로 구현해서 "매번 새로 계산"하는 방식을 택했고,
  결과적으로 dep만 먼저 mark_built하면 app의 과거 invalidation 이력이 사라진다.

즉 **테스트 스위트와 참조 구현은 내가 같은 이해(스펙 해석)로 같이 작성**했기 때문에,
스펙의 빈틈은 테스트와 참조 구현에 그대로 상속된다. producer는 그 테스트를 통과하면 되므로
(2라운드 재시도 안에 정확히 주어진 테스트에 맞춰 수렴), 테스트가 놓친 부분은 producer도
놓친다 — **당연히 verify_ok=False가 될 수 없다**. 반면 codex-critic은 내 스펙 텍스트나
테스트에 얽매이지 않고 "실제로 쓸만한 API/빌드 시스템이라면 이래야 한다"는 도메인
지식으로 채점하므로, 테스트가 못 보는 지점을 정확히 잡아낸다.

이건 리뷰어가 **과도하게 엄격하거나 미보정된 것이 아니라 오히려 정확하게 기능하고 있다는
증거**다. 문제는 리뷰어가 아니라, **"verify_ok를 SCORE의 지상진실로 쓰겠다"는 T-1의 설계
자체가, 그 verify_ok를 만드는 테스트와 SCORE를 매기는 리뷰어가 서로 다른(그리고 리뷰어가
더 넓은) 정보원을 쓴다는 사실과 충돌**한다는 것이다. 테스트 작성자(나)와 스펙 작성자(나)가
같은 사람인 한, 테스트는 스펙의 빈틈을 절대 못 잡는다.

## T-1에 대한 함의

1. **1차·2차에서 관측된 "낮은 SCORE·verify_ok=True" 조합은 리뷰어 미보정의 증거가
   아니다.** 오히려 표본으로 뽑은 3건 전부 리뷰어의 구체적 결함 주장이 독립적으로
   재현 가능했다 — SCORE가 실제 결함을 잡아내고 있다는 정방향 증거.
2. **현재 방식(동일인이 스펙+테스트 작성 → producer가 그 테스트를 목표로 수렴)으로는
   `verify_ok=False`가 원리적으로 희귀하거나 아예 안 나올 수 있다.** 3차 프로토콜을
   그대로(더 어려운 과제로만) 반복해도 같은 구조적 이유로 또 0건이 나올 가능성이 높다.
3. 이 셋 다 우연이 아니라 **재현 가능한(reproducible) 결함**이라는 점도 중요하다 —
   "리뷰어가 트집을 잡았다"가 아니라 실제로 실행하면 정확히 리뷰어가 말한 대로 깨진다.

## 재검토 옵션 (결정 필요 — 실행하지 않음, 정보 제공용)

1. **스펙·테스트를 서로 다른 주체가 작성**: 예를 들어 스펙은 내가 쓰고, acceptance
   테스트는 별도 세션/에이전트(또는 codex 자신)에게 "이 스펙만 보고 테스트를 작성하라"고
   맡긴 뒤, 내가 그 테스트를 리뷰만 하는 방식. 테스트 작성자의 도메인 지식이 스펙 작성자와
   달라지면 테스트가 스펙의 빈틈을 더 많이 잡을 수 있다.
2. **hidden test 도입**: producer에게는 자연어 스펙 + 소수의 예시 테스트만 보여주고,
   `verify_cmd`가 실행하는 실제 채점 테스트는 producer가 못 보는 별도 파일로 분리. 지금
   구조(테스트가 workdir에 그대로 노출됨)에서는 producer가 정확히 그 테스트를 만족하는
   것으로 수렴하는 게 오히려 당연한 결과였다.
3. **T-1의 정의 자체를 재검토**: `verify_ok=False`를 못 얻는 게 방법론적으로 당연하다면,
   SCORE↔verify_ok 혼동행렬이라는 원래 접근 대신, 이번에 한 것처럼 **리뷰어가 제시한
   구체적 재현 스니펫이 실제로 재현되는지를 사후 검증**하는 방식(리뷰어 결함 주장의
   "적중률")을 새로운 캘리브레이션 신호로 채택하는 안. 이번 3건은 전부 적중이었다(3/3).
   표본이 작아 결론 내리긴 이르지만, 기존 계획에 없던 유효한 대안 신호로 보인다.

사용자 판단이 필요한 지점 — 어느 방향으로 갈지(또는 T-1을 이대로 계속 보류할지)는 이
문서가 결정하지 않는다.
