# v2.3.0 · GUI 프로토타입 + 하네스기법·할루시네이션 방지 적용 · 2026-07-04 13:39

## 요약

세 GUI 샘플 비교 결과 **claude(무설치·오프라인·확실히 렌더)를 기반**으로, **lovable의 폴리시**(둥근 파스텔 패널·소스배지·상태색·가독성)를 흡수해 GUI를 재작성하고, **harness-multiagent에 실데이터 연동 프로토타입으로 통합**했다. 동시에 하네스 기법·할루시네이션 방지를 코드에 심고 RULE/HISTORY를 추가했다. 전부 Windows에서 실행 검증.

## 한 것

### 1. GUI (claude 기반 + lovable 보충)
- `harness-multiagent/gui/index.html`. 코믹 베이스(지그재그·점배경·오프셋 그림자) 유지 + lovable식 **둥근 파스텔 패널·소스배지·warn/stop 상태색**.
- **폰트 3단 분리(요청 반영, CDN 0)**: 제목=코믹 / **본문·데이터=가독성 산세리프**(Segoe UI·Malgun Gothic) / **코드·로그·수치=코딩 폰트**(D2Coding→Cascadia→Consolas 폴백).

### 2. 실데이터 프로토타입 (목업 탈출)
- `harness/guiserver.py` + `harness gui` 명령: 표준 라이브러리 http 서버가 `/api/state`로 **실제 limits(codex 라이브 실측)·coach·runs**를 JSON 서빙. GUI가 7초마다 폴링, 파일 직접 열면 mock 폴백.
- 검증: `● LIVE` 배지, codex 5h 1%/7d 24%(live), claude 롤링(max20x 프리셋), 런 6/6 SCORE 9 실시간 표시.

### 3. 하네스 기법 (프로그램 만들 때 적용)
- `orchestrator.HARNESS_TECHNIQUE`: 코딩 워커에 **계획→구현→자가검증(SELF-CHECK)** 강제 주입. 4중 브레이크 유지.
- 검증: build 워커 프롬프트에 주입 확인.

### 4. 할루시네이션 방지
- `ANTI_HALLUCINATION`(전 워커: 날조 금지·근거 제시) + `REVIEW_GUARD`(검수: 환각 명시 지적) + 체크리스트 과잉확신 자동 표시.
- 검증: 주입 확인 + "반드시 동작" 표현이 체크리스트에 실제로 잡힘.

### 5. 거버넌스
- `RULE.md`(코딩툴 원칙·하네스기법·할루시네이션 규칙·리포트 네이밍·릴리스 규칙), `HISTORY.md`(변경 이력, 매 갱신 기록).
- 리포트 네이밍: `버전-작업-날짜-시간`. 헤더에도 동일 표기.
- 버전 2.2 → **2.3.0**.

## 검증 (Windows 11 / Python 3.14)
- ✅ 전 모듈 컴파일 · ✅ `harness gui` 실데이터 서빙 · ✅ GUI 렌더(스크린샷, 폰트/폴리시 확인) · ✅ 콘솔 에러 0 · ✅ 하네스기법·할루시네이션 주입/체크리스트 동작 · ✅ producer-reviewer 6스텝 완주.

## 산출물
- 소스: `harness-multiagent/` (gui/·guiserver.py·RULE.md·HISTORY.md 추가, orchestrator/config/cli 갱신)
- 릴리스: `release/harness-multiagent-v2.3.0.zip`
- 관련 리포트: `v2.3.0-coding-fit-review-2026-07-04-1339.md`(적합성·추가기능)

## 남은 것 (코딩 적합성 리포트 참조)
테스트/린트 게이트 + 파일 실제 적용(P1) — "말하는 도구"에서 "코드를 짜고 돌려 검증하는 도구"로.

---
*작성: 2026-07-04 13:39 · 버전 v2.3.0*
